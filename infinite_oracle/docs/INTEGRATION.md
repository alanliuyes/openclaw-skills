# Integration Guide

## Feishu Integration

1. Create a Feishu webhook
2. Configure the webhook URL in config
3. Enable human-in-the-loop mode

## API Endpoints

- `/status` - Get worker status
- `/trigger` - Manual trigger
- `/config` - Update configuration